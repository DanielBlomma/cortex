# Archive

Closed history rotated out of `manager-log.md` and `handoff-ledger.md` at day
rollover. The live files keep only current state and open items, small enough
for a fresh session to read whole.

Naming: `manager-log-YYYY-MM-DD.md`, `handoff-ledger-<date-range>.md`.
