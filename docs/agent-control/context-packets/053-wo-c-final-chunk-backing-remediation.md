# WO-C Final Chunk-Backing Remediation

## Objective

Close the same independent reviewer's final two WO-C majors on accepted base
`482f196`. This packet is additive to and governed by packets 051–052. Work
only in `/Users/danielnilsson/GIT/cortex-wo-c-2.5.2`. Preserve accepted
WO-A/WO-B bytes, package version 2.5.2, dependencies, and lockfiles. No commit,
push, merge, release, publish, deployment, acceptance, WO-D, or additional
broad review is authorized.

## Exact Majors

1. Bind every selected/output-affecting chunk's semantic fields—body,
   signature, kind, and description—to the exact live or HEAD-backed source
   span or to a fresh canonical reconstruction from the repository's existing
   parser path. ID, owner, line, and document-byte checks alone are
   insufficient. Reject coherent ID-preserving fabrication of each semantic
   field even for zero-finding/profile-only reviews. Use an existing robust
   canonical parser or ContextData reconstruction when available; do not
   invent fragile substring parsing. Add real temporary-Git/live-source tests
   and repeat all selected-profile/output-affecting backing checks before
   output.
2. After the final code and documentation edit, execute the definitive
   expanded-config update and exact 22-artifact pattern loop/digests, restore
   the normal config byte-for-byte, and run the final normal update, complete
   backing/manifests validator, accepted-base parity, exact 18-state replay,
   doctor, watch/status, and diff checks. Record current post-remediation
   totals, remove stale/pre-remediation labeling, and make no covered edit
   afterward.

## Validation and Handoff

Run build and focused affected tests, plus one proportional MCP gate if the
runtime integration warrants it. Freeze code and control documents before the
expanded/normal evidence sequence. Return the exact frozen candidate to the
same reviewer for narrow read-only verification of only these two majors.
Only the manager may accept WO-C or unblock WO-D.
