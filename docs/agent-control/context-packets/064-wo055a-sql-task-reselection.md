# WO-055A-SQL Authorized Task Reselection

## Objective

Replace only the invalid frozen SQL task before any gold, treatment,
recurrence, scoring, reveal, or solution output exists. Select one fresh real
SQL maintenance task whose unchanged frozen scope yields at least two
independent owners through Cortex's accepted packaged SQL ingest path, then
write a new contained private bundle and WO-055A v3 plan for independent
review.

The user explicitly authorized this integrity-preserving reselection after
Packet 063 proved that `wo055a-sql-001` yields three documents but zero
accepted parser chunks. The failed binding remains retired audit evidence and
must never be silently reused.

## Starting State

- Branch: `feature/wo055a-sql-reselection`.
- Worktree: `/Users/danielnilsson/GIT/cortex-wo055a-sql-reselection`.
- Start only after the manager's Packet 064 control commit is HEAD.
- Use a fresh packet-only session. Read repository `AGENTS.md`, Packet 062,
  Packet 063, this packet, and the direct references below completely. Run
  Cortex search/rules/impact before selection conclusions.
- No gold, baseline/candidate treatment, recurrence, score, patch, reveal,
  model/provider, planner, telemetry, or solution-agent output exists or is
  authorized.
- Do not modify the WO-055A five-file candidate worktree. This work order owns
  outside-Git selection artifacts only.

## Direct References

1. `docs/agent-control/context-packets/062-wo055a-blind-dialect-fixture-lock.md`
   — original task freshness, private-byte separation, contamination, and
   accepted-index contract.
2. `docs/agent-control/context-packets/063-wo055a-accepted-index-review-repair.md`
   — accepted ingest/index and review-fix contract.
3. `scaffold/scripts/indexing.mjs`, `scaffold/scripts/ingest.mjs`,
   `scaffold/scripts/parsers/sql.mjs`, and the imported ingest modules — sole
   accepted SQL availability authority.
4. Read-only v2 generator at
   `/Users/danielnilsson/GIT/cortex-wo055a-dialect-fixture-lock/benchmark/bootstrapbench/wo055a-freeze-dialect-fixtures.mjs`, expected SHA-256
   `a75dbf2f7c2b50b21ebe82eed5710122b75c19de7a2fd94da8824e45e325a758`,
   for the current closed v2/v3 plan schema only. Do not edit that worktree.
5. Dedicated run root
   `/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1` at mode `0700`.
   Existing v1/v2 bundle, plan, registry, fetch record, repo, and index paths
   are audit inputs. Never print task bytes.

## Exact Owned Scope

No tracked file may be added or changed by the selection agent. Create only
new immutable outside-Git artifacts under:

`/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1/sql-reselection-v1/`

The directory is mode `0700`; every private JSON record is regular,
single-link, atomically written, immutable, and mode `0600`:

1. `sql-task-selection-v1.json` — closed selected binding, source catalog,
   accepted ingest counts/hashes, independent owner identities/locations,
   contamination result, and exact network execution metadata; no solution
   patch.
2. `private-task-bundle-v2.json` — the prior 13 exact task entries plus only
   the replacement SQL task bytes. Never overwrite or delete v1.
3. `freeze-plan-v3.json` — the prior 13 bindings plus only the replacement SQL
   binding and its accepted owner selectors.
4. `git-fetch-execution-record-v2.json` — exactly 14 bindings with the retired
   SQL fetch replaced by the selected SQL repository/object/issue provenance.
5. `contamination-registry-v2.json` — recomputed authorities plus the retired
   SQL task identity/hash/aliases/base tuple as an explicit exclusion.
6. A clean detached SQL repository and accepted-index workspace below this
   reselection root. They remain outside Git and are not package inputs.

Any tracked edit, production change, task replacement outside SQL, or need to
change the accepted SQL parser is a stop condition.

## Selection Contract

Select exactly one replacement binding, with a new visible ID such as
`wo055a-sql-002`, before inspecting any solution patch or post-change source.
The task must:

- be an exact public GitHub issue/request for a concrete SQL maintenance or
  behavior change in a real repository;
- use exact UTF-8 title/body task bytes within the accepted bound, stored only
  in the v2 private bundle and represented publicly/diagnostically only by
  byte count and SHA-256;
- bind a clean detached checkout at an immutable 40-hex base and root tree,
  selected without opening a solution PR, diff, patch, or solution commit;
- have a non-empty sorted canonical `.sql` source scope grounded in the issue;
- pass the accepted packaged ingest unchanged and yield at least two distinct
  parser chunks whose owner identities are independently derived from the SQL
  declaration name/kind/content, not merely from two spans;
- retain real accepted documents, chunks, relations, and graph inputs in the
  non-dialect index with dialect output and embeddings excluded;
- be absent by task ID/hash, canonical issue identity/aliases, and base/patch
  tuple from all Packet 062 authorities, all current WO-055A tasks, and the
  explicitly retired `wo055a-sql-001` binding;
- use only HTTPS GitHub repository/object and issue endpoints recorded in the
  execution record. No arbitrary network service is permitted.

Discovery may inspect public repository metadata, unchanged source, tests,
and issue text. It may not inspect linked solution PRs, commits after the
selected base for solution discovery, patches, generated candidate evidence,
or any gold/mechanism rubric. A failed candidate is recorded privately and
cannot be silently presented as selected.

## Integrity Rules

- Preserve the exact 13 non-SQL public/private entries byte-for-byte when
  creating bundle v2 and plan v3. The only allowed semantic delta is retiring
  SQL-001 and adding SQL-002.
- Never overwrite v1/v2 audit artifacts. Use new paths and exclusive atomic
  creation. Reject symlinked/hard-linked/path-redirected inputs or outputs and
  validate parent identities around reads/writes.
- Do not print issue title/body, private bundle fields, or any task text in
  logs, diagnostics, tracked docs, or chat. Return only identity, bytes, hash,
  repository/base/tree, source paths, counts, and owner/index hashes.
- The selected task is provisional until three independent read-only reviewers
  return GO. Packet 063 may resume only after manager acceptance.
- No task may be replaced after WO-055B or WO-055C begins.

## Required Validation

At minimum:

1. Verify all input/output paths, modes, types, link counts, byte bounds, and
   hashes before and after stable descriptor reads.
2. Verify bundle v2 and plan v3 contain exactly 14 sorted tasks and that the 13
   non-SQL entries match their predecessors exactly.
3. Verify the selected repo is clean/detached at exact base/root tree and every
   scoped source is tracked, regular, single-link, UTF-8, canonical, and
   unchanged after ingest.
4. Run accepted packaged ingest twice from clean contained workspaces and prove
   byte-identical document/chunk/relation/graph-input/index bytes.
5. Prove at least two independent SQL declaration owners and adversarially
   reject one owner split into two spans.
6. Recompute all contamination authorities and prove zero collisions,
   including explicit collision with retired SQL-001.
7. Prove the execution record contains only the selected GitHub HTTPS
   repository/object/issue access and zero forbidden capability paths.
8. Run relevant SQL/parser/ingest, filesystem containment, contract/evaluation,
   JSON/canonicalization, and `git diff --check` gates. Worktree tracked status
   must remain clean.
9. Run Cortex pattern evidence on the unchanged accepted authorities as
   applicable; report exact legacy-scaffold N/A for update/doctor/watch if it
   remains unchanged.

## Independent Review

Before manager acceptance, obtain three fresh read-only reviews:

1. Selection/Contract — real maintenance binding, accepted parser owners,
   deterministic index, and exact 13+1 replacement semantics.
2. Security/Containment — private bytes, path/link/race containment,
   contamination completeness, and bounded GitHub-only egress.
3. Validation/Blindness — no solution/gold/treatment/candidate access, retired
   binding exclusion, reproducibility, and no tracked/package mutation.

All three must return GO with no blocker or major finding. Findings are fixed
and re-reviewed without changing another task.

## Return

Return the selected SQL task ID, issue identity hash (not text), task byte
count/hash, repository/base/root tree, source paths/catalog hashes, two or more
independent owner identities/locations, accepted ingest/index counts and
hashes, contamination/fetch records, bundle/plan paths/modes/bytes/hashes, test
totals, and exact N/A. Do not commit or begin Packet 063 repair; stop for
independent review.
