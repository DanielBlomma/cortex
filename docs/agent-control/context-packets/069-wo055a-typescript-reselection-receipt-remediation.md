# WO-055A-TypeScript Receipt And Negative Remediation

## Objective

Retain the viable `wo055a-typescript-002` binding from Packet 068 and repair
only its rejected creator/receipt evidence. Execute every mandatory
filesystem-race, special-file, exclusive-create, and post-validation-drift
negative against the production creator/validator path, record only observed
results, and exclusively create one fresh accepted
`typescript-reselection-v1` root for independent review.

This work order does not authorize discovery, reselection, task-byte changes,
source/base changes, generator integration, parser changes, or a lowered gate.

## Starting State

- Worktree:
  `/Users/danielnilsson/GIT/cortex-wo055a-typescript-reselection`, branch
  `feature/wo055a-typescript-reselection`, fast-forwarded to the Packet 069
  control commit before work starts.
- Fresh packet-only session. Read repository `AGENTS.md`, Packets 062-069,
  current manager/handoff rows, and direct authorities completely. Run Cortex
  search/rules/impact before conclusions.
- Tracked status is clean and must remain clean. No commit.
- The exact accepted destination
  `/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1/typescript-reselection-v1`
  is absent.
- Preserve the rejected audit root intact at
  `/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1/typescript-reselection-v1.rejected-unexecuted-negative-receipts-20260829`.
  It is untrusted input because its receipt overclaims unexecuted negatives.

## Immutable Binding

- ID `wo055a-typescript-002`.
- Issue identity SHA-256
  `3a783e15fa522e36105e44977768ced05e621d2a8f16c840c4d4d0d640128d2c`.
- Task: 1,059 bytes, SHA-256
  `06a227c99135fd8406da48660f76f989f73eb54aad49cacabcbdece5c17c04a5`;
  never print its bytes.
- Repository `https://github.com/tinylibs/tinybench.git`, base
  `972968eba6c796e00d59c455243e5f958c3ba052`, tree
  `541df519ff7a14fe1b4889b929764c259a01c116`.
- Source `src/task.ts`, 23,109 bytes, SHA-256
  `323654cdb98abe98c23827ba7120463d2a042c536857aa06adea9cd739910542`,
  catalog SHA-256
  `f232498158bb3e2fc9299e24e21a59d97401abd5ea610bff63e1d903a44256f6`.
- Semantic owners:
  `owner-v4:cf44fee89e26fe3313dfbe1c6264367992daa48ad01931da96f570d7b2da6e0c`
  at
  `location-v3:be0ad0c9c48eb6ff5ca6a4e7e403d1100821754860404fff249c41bc0d9447ff`,
  and
  `owner-v4:afa858d77518cc8ab5fab63174a22d39c1f1a883e2c22f3e3056f543732ca619`
  at
  `location-v3:f943f23ce1d06582013e08ab8f206f0380703e7f7aaa162993b88adfcade4d62`.
- Both accepted replays: 1 document, 23 chunks, 26 relations, 21 graph
  inputs, zero parse errors; index SHA-256
  `628948a48fe1cdfe234f5ef592589417bb455cc6fefb27bd7e5bb013e0dc6ab3`,
  payload SHA-256
  `e286d31805a1e4c3856813d06a976492ff6eaa25afdf6cc7f597a706712e951f`,
  replay SHA-256
  `f2f09aec63932a12784a62b302e7082f04b47385bb00b3efafc2919928f6e6d`.
- SQL-002 and all other 13 bindings remain byte-exact. Generator compatibility
  remains truthfully closed `false` and integration remains unauthorized.

Any drift in these facts is NO-GO. Do not refetch or substitute a task to make
the remediation pass.

## Exact Owned Scope

No tracked edit. Outside Git, create a new creator/validator source and staged
records only under a fresh mode-`0700` temporary sibling. On success atomically
and exclusively commit it to the exact accepted destination. Preserve every
failed attempt under a unique rejected audit name; never overwrite or delete
the Packet 068 rejected root.

The final root has the exact Packet 068 record set, mode `0600`, plus any
closed negative-execution record needed to bind actual invocations. Recreate
all records from trusted immutable inputs; do not copy the rejected receipt or
accept its claimed pass values.

## Findings To Close

The retained Packet 068 creator/receipt recorded mandatory negatives as passed
without executing them. The remediation must drive the same exported
low-level creator/validator helper used by the CLI/root path and observe:

1. two concurrent exclusive creators, with at most one winner and a loser that
   fails closed without mutation;
2. parent directory rename/rebind between validation and commit;
3. existing destination rejection;
4. symlink input/output/parent rejection;
5. hard-linked private record rejection;
6. FIFO and other special-file rejection without blocking;
7. post-validation source, runtime inventory, receipt input, and output drift;
8. wrong mode, wrong link count, moved root, stale authority/HEAD, stale
   generator hash, and creator hash drift; and
9. canonically rehashed forged negative receipts, request/denial receipts,
   runtime inventories, or self-declared pass counts.

A narrow deterministic precommit injection is allowed only on the exported
test helper and must not be reachable from the normal creator CLI. Every
receipt entry binds exact argv, exit, stdout/stderr hashes, pre/post inventory,
mutation target, observed denial code, creator hash, Packet 069 HEAD, and final
output cross-hashes. A test that merely constructs JSON is not execution
evidence.

## Required Gates

- Run all nine negative families above through the actual helper and require
  fail-closed observed results.
- Run the normal exclusive creator from empty destination, then validate the
  exact root twice with no mutation and prove a second normal creator loses.
- Reprove exact 13+1, every SQL-002 hash, two accepted replay parity, two
  semantic owners, five-authority zero contamination, two exact GitHub-only
  requests, 11 exact denials, Node/runtime inventories, modes/types/links, and
  compatibility `false`.
- Run focused creator/receipt tests plus relevant filesystem, TypeScript
  parser/ingest, dialect contract/evaluation/runtime tests from an unchanged
  dependency-equipped authority worktree. If an environment gate is N/A,
  prove why and retain the two packaged accepted replay results; do not count
  missing dependencies as passes.
- Run syntax, canonical JSON, `git diff --check`, exact clean status, and Cortex
  pattern evidence or exact unchanged/outside-Git N/A.

## Independent Review And Return

Do not commit or integrate. Return exact non-private hashes, modes, counts,
actual negative invocation totals/results, normal validation results, tests,
status, and N/A, then stop for three fresh read-only reviews:

1. Contract/Selection — immutable binding, exact 13+1/SQL preservation,
   semantic owners, deterministic replay, and honest compatibility.
2. Security/Containment — actual race/special-file/drift execution, trusted
   receipts/runtime, private-byte safety, and exclusive root lifecycle.
3. Validation/Blindness — no reselection/solution/gold/treatment/candidate
   access, complete gates, reproducibility, and no tracked/package mutation.

All three must return GO with no blocker or major finding. Any false receipt,
unexecuted claimed negative, task/binding drift, or missing mandatory gate is
NO-GO and remains preserved for another fresh repair; never lower the gate.
