# WO-055A Accepted-Index Review Repair

## Objective

Repair the frozen WO-055A candidate after unanimous independent NO-GO, finish
the same 14 task bindings through the accepted legacy ingest/index path, and
return a new review-ready five-file candidate without beginning gold,
treatments, recurrence, scoring, or reveal.

This is the fresh-session remainder of Packet 062. Packet 062 remains the
authoritative scope and acceptance contract; this packet contains only the
durable repair state and narrowed remaining work.

## Starting State

- Work only in `/Users/danielnilsson/GIT/cortex-wo055a-dialect-fixture-lock`
  on `feature/wo055a-dialect-fixture-lock`.
- Read Packet 062 completely, then this packet and the exact direct references
  named by Packet 062. Run Cortex search/rules/impact before code conclusions.
- The candidate is uncommitted. Exactly five tracked candidate files remain in
  scope; do not add production, dependency, package, ownership, lockfile,
  public API, or other tracked changes.
- Preserve all 14 existing task IDs, exact private task bytes, repository/base
  bindings, and blindness. Never open or print private task text in chat or
  tracked outputs.
- The prior repair session stopped at a safe boundary after context
  compaction. It did not write new public fixture, report, or attestation
  bytes.

## Independent Review Findings To Close

All three required reviewers returned NO-GO. The repaired candidate must close
every item before re-review:

1. Replace the synthetic owner-only index with the accepted packaged legacy
   path `scaffold/scripts/indexing.mjs` through `ingest.mjs`. Bind the real
   accepted documents, parser chunks, relations, DB-import graph inputs, and
   explicit disabled embedding/conventions/guidance state. Exclude the dialect
   sidecar and make the frozen index usable by WO-055C/D.
2. Derive implementation-owner identity independently from span/location.
   Reject one owner split into two spans, recompute the identity in the frozen
   validator, and cover the negative case adversarially.
3. Close the retrieval budget to the accepted lexical/graph-only contract:
   `top_k: 50`, `query_vector: null`, semantic/model disabled, no unknown or
   reordered data.
4. Canonically derive and validate maintenance/issue/base-patch identities;
   do not trust opaque plan-supplied hashes for freshness.
5. Recompute and bind the five prescribed contamination authority paths,
   inventories, counts, byte totals, and hashes. Reject placeholders, zeroed
   authorities, and forged identities.
6. Export a closed detached-attestation validator with exact keys and complete
   fixture/report/generator/private/external-repository cross-binding.
7. Exercise two full generator runs to detached temporary public outputs and
   compare fixture, report, and attestation bytes exactly.
8. Contain plan/private/registry/index/execution artifacts under the dedicated
   outside-Git run root, constrain public outputs to the five owned paths or a
   verified detached temporary review root, prevent arbitrary overwrite, and
   use descriptor/parent-identity checks against link/path-swap races.
9. Replace self-declared network/forbidden-call counters with closed execution
   evidence and capability absence. Only the 14 named Git HTTPS origins/base
   objects are allowed; model/provider/planner/telemetry/solution paths remain
   absent.
10. Remove duplicate object keys/validator calls and add focused negative tests
    for every repaired boundary.

## Durable Outside-Git State

The dedicated run root is
`/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1` at mode `0700`.
Treat the following as direct references by path and metadata only; never print
private task bytes:

- `freeze-plan-v2.json`: mode `0600`, 11,818 bytes,
  SHA-256 `968bd904ec0fdc7fc8536644938be9641a35a84c5e08337aefd1619070e953a3`.
- `contamination-registry-v1.json`: mode `0600`, 407,579 bytes,
  SHA-256 `fcfadbb45242bea38108a44fa36633621fcee0352617740f20761d16fc2c605e`.
- `git-fetch-execution-record-v1.json`: mode `0600`, 4,964 bytes,
  SHA-256 `9a6e7eb04fb66ab818f92077adfc7879e91cde8e3e5e0c3ef0bcdf1d88f7eddc`.
- `private-task-bundle-v1.json`: mode `0600`, 8,427 bytes,
  unchanged SHA-256
  `5cd872985bd2846a15df2fe788f5696cb9879141fcadde11c3105ee84986c211`.
- Bash provisional accepted index
  `indexes/wo055a-bash-001/non-dialect-index-v2.json`: mode `0600`,
  10,295 bytes, SHA-256
  `252c9ceb721446262aa204ff06cf14a039bc29a65fd7d2142909db93d3dca305`.

The contamination rehash covers the five prescribed authorities at file
counts `5,384 + 1 + 82 + 2 + 0`. The v2 plan contains the same 14 task
bindings and no plan-controlled retrieval/counter/network assertions. The
fetch record binds the 14 Git HTTPS repositories.

## Immediate Resume Point

The v2 generator passes `node --check`. Its first accepted ingest succeeded for
Bash with two documents, two parser chunks, seven relations, and 21 graph
inputs, while excluding the dialect sidecar. Freeze then failed closed because
both accepted chunks were in `lib/bats-core/warnings.bash`; the second selected
file emitted no parser chunk.

Change only the outside-Git Bash owner selectors for the same task to the two
independent accepted parser owners already located at spans `7-27` and `30-34`.
Do not change the task, repository, base, source scope, or private bytes. Then
continue the identical accepted-ingest gate across all 14 tasks. Any family
without two independent accepted owners is a real NO-GO; do not substitute a
task or lower the gate.

## Owned Tracked Scope

Exactly the Packet 062 five-file candidate:

1. `benchmark/bootstrapbench/wo055a-freeze-dialect-fixtures.mjs`
2. `benchmark/bootstrapbench/fixtures/wo055a-dialect-v1/frozen-fixture-v1.json`
3. `benchmark/bootstrapbench/fixtures/wo055a-dialect-v1/artifact-attestation-v1.json`
4. `tests/bootstrapbench-wo055a-dialect-fixtures.test.mjs`
5. `docs/agent-control/wo055a-dialect-fixture-report.md`

Do not commit until the manager requests it after all three final re-reviews
return GO with no blocker or major finding.

## Required Return

Run every Packet 062 validation gate after the v2 public artifacts are frozen,
including focused negatives, accepted contract/evaluation/runtime/query/
cleanup tests, root and MCP suites, pack dry-run, diff checks, and Cortex gates
or exact packet-valid N/A. Report exact 14-family ingest/index counts and
hashes, owner proofs, contamination/fetch execution evidence, private metadata
without task bytes, deterministic rerun hashes, package inventory, and all test
totals. Then stop uncommitted for the three independent re-reviews.
