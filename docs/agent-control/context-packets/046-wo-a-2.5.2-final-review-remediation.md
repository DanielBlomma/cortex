# WO-A 2.5.2 Final Review Remediation

## Objective

Close the two major and one minor finding from the packet-045 five-role review,
refresh final durable evidence, and return WO-A 2.5.2 for focused re-review.
Do not start WO-B.

## Starting State

- Target: `/Users/danielnilsson/GIT/cortex-wo-a-2.5.2`, branch
  `feature/wo-a-repo-conventions-2.5.2`, baseline
  `1cbb4f0522db9114be25ce6b779ae30b0c8b2b06`, version 2.5.2.
- Read packet 045, current results, complete diff, old review gates, and
  `scaffold/AGENTS.md`. Use only checkout-local `node bin/cortex.mjs`.
- Code Quality returned PASS. Contract and Validation independently found the
  same durable-evidence major. Security found one loader-boundary major.
  Integration found one packed-report minor. No other finding remains.
- No commit, merge, rebase, release/version/dependency change, acceptance, or
  WO-B work.

## Fix-Now Findings

### 1. Major — root conventions loader error boundary

- `bin/cli/query-command.mjs` currently loads the project query runtime before
  the convention-specific handler can sanitize failures. Missing/unreadable or
  import-failing runtime errors reach the root catch and expose absolute paths
  and raw loader details; `--json` does not receive the bounded versioned error
  envelope.
- At the root shim boundary, detect the `conventions` command and catch runtime
  resolution/import failures. Reuse the same bounded sanitized public error
  contract: JSON mode emits the deterministic bounded JSON envelope; text mode
  emits only a generic bounded diagnostic. Do not expose absolute paths, raw
  import text, control characters, source content, or rejected input.
- Add root-level missing and broken runtime tests for JSON/text, including a
  control-bearing temporary directory. Prove no persistence and preserve normal
  query/runtime error behavior for other commands.

### 2. Major — durable exact final evidence

- Replace future-tense results language with completed evidence.
- The durable results/control record must contain a stable in-repository
  locator, the exact final changed/covered accounting and file list, expanded
  and normal totals, artifact/digest values, config hashes, checksum/profile/
  manifest results, doctor/watch/diff/status, and exact reproduction commands.
- `/tmp` artifacts may be cited as ephemeral supporting output only; a fresh
  manager must be able to reproduce and verify the gate from durable records
  after `/tmp` cleanup.
- Because covered records change, finalize them before repeating the immutable
  pattern → config restoration → final normal update/checksum/profile →
  doctor/watch/diff/status sequence. Do not edit covered files afterward.

### 3. Minor — packed mode-count report

- `tests/packed-filesystem-containment.test.mjs` asserts the current 424-entry
  inventory with 403 `0644` and 21 `0755` files but emits stale `399/21` JSON.
- Derive the reported counts from the checked inventory or use the same single
  verified constants. Rerun the affected packed gate.

## Validation And Re-review

- Run targeted loader/root/packed tests, build, pure/focused, and affected MCP;
  rerun broader gates only if affected.
- Reconcile version 2.5.2, unchanged dependencies, lifecycle/runtime parity,
  deterministic live output, and all previous packet-045 evidence.
- Perform the final immutable evidence sequence after every covered edit.
- Return exact final accounting and hashes without recording acceptance.
- Fresh Security, Contract, Integration, and Validation re-review is required;
  Code Quality may be reused only if the implementation change is demonstrably
  limited, otherwise re-review it too. WO-B remains blocked.
