# WO-055A-TypeScript Authorized Task Reselection

## Objective

Replace only the invalid frozen TypeScript task before any gold, treatment,
recurrence, scoring, reveal, or solution output exists. Select one fresh real
TypeScript maintenance task whose unchanged frozen scope yields at least two
independent semantic owners through Cortex's accepted packaged TypeScript
ingest path, then write a new contained private bundle and freeze plan for
independent review and a later integration work order.

The user explicitly authorized this integrity-preserving reselection after
Packet 067 proved that `wo055a-typescript-001` yields two documents but zero
accepted parser chunks and two parse errors on unsupported `override`
constructs. The failed binding remains retired audit evidence and must never
be silently reused.

## Starting State

- Branch: `feature/wo055a-typescript-reselection`.
- Worktree:
  `/Users/danielnilsson/GIT/cortex-wo055a-typescript-reselection`.
- Start only after the manager's Packet 068 control commit is HEAD.
- Use a fresh packet-only session. Read repository `AGENTS.md`, Packets
  062-068, the current manager/handoff rows, and the direct references below
  completely. Run Cortex search/rules/impact before selection conclusions.
- No gold, baseline/candidate treatment, recurrence, score, patch, reveal,
  model/provider, planner, telemetry, or solution-agent output exists or is
  authorized.
- Do not modify the WO-055A five-file candidate worktree or the accepted
  SQL-002 root. This work order owns new outside-Git selection artifacts only.

## Direct References

1. `docs/agent-control/context-packets/062-wo055a-blind-dialect-fixture-lock.md`
   through
   `docs/agent-control/context-packets/067-wo055a-sql-bridge-fail-closed-closure.md`
   — task freshness, blindness, accepted-index, SQL-002, receipt, runtime,
   identity, and fail-closed contracts.
2. `scaffold/scripts/indexing.mjs`, `scaffold/scripts/ingest.mjs`,
   `scaffold/scripts/parsers/javascript.mjs`, and every module imported by the
   accepted TypeScript ingest path — sole availability authority.
3. Read-only current candidate generator at
   `/Users/danielnilsson/GIT/cortex-wo055a-dialect-fixture-lock/benchmark/bootstrapbench/wo055a-freeze-dialect-fixtures.mjs`, expected SHA-256
   `4d40f092005eeed8efb20d6fc84555139ed5634f5f24ebf06d3bde5f1badc882`.
   It is an audit and compatibility input only; do not edit that worktree.
4. Accepted SQL-002 root
   `/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1/sql-reselection-v2`
   and its exact plan, bundle, fetch, registry, selection, index, creator, and
   receipt records. Preserve those bytes and paths; never print private task
   fields.
5. Dedicated parent run root
   `/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1` at mode `0700`.
   Existing bundles, plans, registries, fetch records, repositories, indexes,
   and rejected audit roots are inputs only and must not be overwritten,
   deleted, renamed, or weakened.

## Exact Owned Scope

No tracked file may be added or changed by the selection agent. Create only a
new, exclusively committed outside-Git root:

`/Users/danielnilsson/.cache/cortex/wo055a-dialect-v1/typescript-reselection-v1/`

The root is a real, non-symlink directory at mode `0700`. Every private record
is a regular, single-link file, atomically and exclusively written at mode
`0600`. Retain the creator source and a creation receipt that bind all trusted
inputs, outputs, runtime files, requests, denials, and exact Packet 068 HEAD.
The root contains at minimum:

1. `typescript-task-selection-v1.json` — closed selected binding, source
   catalog, accepted ingest counts/hashes, distinct semantic owner identities
   and locations, contamination result, and exact network execution metadata;
   no solution patch.
2. `private-task-bundle-v3.json` — the prior 13 exact task entries plus only
   the replacement TypeScript task bytes. Never overwrite bundle v1/v2.
3. `freeze-plan-v4.json` — the prior 13 exact bindings plus only the replacement
   TypeScript binding and accepted semantic-owner selectors.
4. `git-fetch-execution-record-v3.json` — exactly 14 bindings with only the
   retired TypeScript fetch replaced by selected TypeScript provenance.
5. `contamination-registry-v3.json` — recomputed authorities plus the complete
   retired TypeScript identity/hash/issue aliases/base tuple as explicit
   exclusions.
6. `generator-compatibility-v1.json` — a closed, hash-bound comparison against
   the current candidate generator and SQL-002 root. It must state whether the
   new bundle/plan can be consumed byte-for-byte without tracked edits and, if
   not, enumerate the exact schema/routing differences for a later, separately
   authorized integration bridge. Compatibility is evidence, not permission
   to edit the generator.
7. A clean detached TypeScript repository and two clean accepted-index replay
   workspaces below this root. They remain outside Git and package inputs.

Any tracked edit, production/parser change, task replacement outside
TypeScript, SQL-002 byte/path drift, or need to lower an availability gate is a
stop condition.

## Selection Contract

Select exactly one replacement binding, with visible ID
`wo055a-typescript-002`, before inspecting any solution patch or post-change
source. The task must:

- be an exact public GitHub issue/request for a concrete TypeScript maintenance
  or behavior change in a real repository;
- use exact UTF-8 title/body task bytes within the Packet 062 byte bound,
  stored only in bundle v3 and represented elsewhere only by byte count and
  SHA-256;
- bind a clean detached checkout at an immutable 40-hex base and root tree,
  selected without opening a solution PR, diff, patch, or solution commit;
- have a non-empty, sorted canonical `.ts`/`.tsx` source scope grounded in the
  issue and free of constructs rejected by the accepted parser at that base;
- pass the unchanged packaged accepted ingest twice and yield at least two
  distinct accepted TypeScript declaration owners. Owner identity is the
  canonical source path, accepted declaration kind, normalized declaration
  name, and normalized declaration header/signature, independent of span,
  chunk ID, chunk body, or body splitting;
- adversarially reject one owner represented by adjacent, overlapping,
  nested, or duplicated chunks as satisfying the two-owner gate;
- retain real accepted documents, chunks, relations, and graph inputs in the
  non-dialect index with dialect output and embeddings excluded;
- be absent by task ID/hash, canonical issue identity/aliases, repository/base
  tuple, and patch/solution identity from every Packet 062-067 authority,
  every current WO-055A task, and retired `wo055a-typescript-001`;
- preserve SQL-002's exact issue, task bytes/hash, repository/base/tree,
  sources, owner selectors, bundle fields, fetch provenance, registry facts,
  and accepted-index facts without refetching or rewriting them; and
- use only HTTPS GitHub repository/object and issue endpoints recorded in the
  execution record. No arbitrary network service is permitted.

Discovery may inspect public repository metadata, unchanged source, tests,
and issue text. It may not inspect linked solution PRs, commits after the
selected base for solution discovery, patches, generated candidate evidence,
or any gold/mechanism rubric. Record every failed candidate privately without
task text and never silently present one as selected.

## Integrity And Receipt Rules

- Preserve the exact 13 non-TypeScript public/private entries byte-for-byte.
  The only allowed semantic delta is retiring TypeScript-001 and adding
  TypeScript-002.
- Derive the prior authority from the exact accepted SQL-002 v3 records; do not
  reconstruct it from chat, logs, or the public fixture.
- The retained creator must exclusively build the final root from a staging
  sibling, validate parent/device/inode identities before commit, and reject
  existing destinations, symlinks, hard links, special files, parent rebinds,
  concurrent creation, stale inputs, runtime drift, or post-validation change.
- Trust exact closed request roles, canonical argv, tool logical paths/hashes,
  response/object hashes, zero exits, denial capabilities, creator hash, Node
  executable/version/hash, complete imported runtime inventories, and all
  input/output cross-hashes. Canonically rehashed forged self-declarations
  must fail.
- Never print issue title/body, bundle fields, or any task text in logs,
  diagnostics, tracked docs, or chat. Return only identity hashes, byte counts,
  repositories, bases/trees, source paths, owner identities/locations, counts,
  hashes, modes, and gate totals.
- The selected task is provisional until three independent read-only reviewers
  return GO. Packet 063 must not resume until manager acceptance and any needed
  integration bridge has independently passed.
- No task may be replaced after WO-055B or WO-055C begins.

## Required Validation

At minimum:

1. Verify every input/output path, mode, type, link count, byte bound, and hash
   before and after stable descriptor reads.
2. Verify bundle v3 and plan v4 contain exactly 14 sorted tasks, the 13
   non-TypeScript entries match their accepted predecessors exactly, and the
   delta is precisely TypeScript-001 to TypeScript-002.
3. Verify SQL-002 and every associated private/public binding remain exact.
4. Verify the selected repo is clean/detached at the exact base/root tree and
   every scoped source is tracked, regular, single-link, UTF-8, canonical, and
   unchanged after both ingests.
5. Run the accepted packaged ingest twice from separate clean contained
   workspaces and prove byte-identical document/chunk/relation/graph-input and
   deterministic non-dialect index bytes.
6. Prove at least two distinct semantic owners and adversarially reject
   same-owner span/body/chunk duplication, unsupported syntax, parse errors,
   zero owners, and one owner.
7. Recompute all contamination authorities and prove zero collisions,
   including exact collisions with every retired TypeScript-001 identity
   dimension and forbidden collision with SQL-002.
8. Prove the execution record contains only the selected GitHub HTTPS
   repository/object/issue access, exact trusted receipts, and zero forbidden
   capability paths.
9. Validate the final root twice, moved-root rejection, creator/generator/
   authority/runtime binding, race and special-file negatives, and the closed
   compatibility record.
10. Run relevant JavaScript/TypeScript parser and ingest, filesystem
    containment, contract/evaluation, canonical JSON, syntax, and
    `git diff --check` gates. Worktree tracked status must remain clean.
11. Run Cortex pattern evidence on unchanged accepted authorities as
    applicable; refresh is N/A because this work order permits no tracked
    change. Report exact dependency or legacy-scaffold N/A rather than
    weakening a gate.

## Independent Review

Before manager acceptance, obtain three fresh read-only reviews:

1. Selection/Contract — real maintenance binding, distinct accepted parser
   owners, deterministic index, exact 13+1 replacement, SQL-002 preservation,
   and explicit generator compatibility.
2. Security/Containment — private bytes, path/link/race containment,
   contamination completeness, trusted receipts/runtime, and bounded
   GitHub-only egress.
3. Validation/Blindness — no solution/gold/treatment/candidate access,
   retired identity exclusion, reproducibility, and no tracked/package
   mutation.

All three must return GO with no blocker or major finding. Findings are fixed
and re-reviewed without changing another task. If a valid task cannot satisfy
the accepted ingest and two-owner gates, return NO-GO; do not change the parser
or lower the contract.

## Return

Return the selected TypeScript task ID, issue identity hash (not text), task
byte count/hash, repository/base/root tree, source paths/catalog hashes, two or
more semantic owner identities/locations, accepted ingest/index counts and
hashes, contamination/fetch/receipt/runtime facts, bundle/plan/compatibility
paths/modes/bytes/hashes, exact tests, status, and N/A. Do not commit, edit the
candidate generator, integrate the selection, or begin Packet 063; stop for
independent review.
