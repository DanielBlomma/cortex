# Risk Register

Every unresolved security, privacy, data, delivery, or context risk gets a row.
A risk stays here until it is mitigated with evidence or explicitly accepted by
the manager; the Status column carries the current truth, not the original fear.

| ID | Area | Risk | Impact | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|
| R1 | Context | Manager or agents lose state through context overflow | Inconsistent decisions and duplicated work | Use this folder plus context packets and handoff ledger | Control Manager | Mitigated baseline |
| R2 | Deploy | GitHub Pages is disabled or misconfigured while site PRs merge successfully | Website returns 404 despite green local builds | Verify Pages `build_type=workflow` and run a live smoke after Pages-affecting merges | Ops/Release Reviewer | Mitigated baseline; Pages enabled 2026-06-15 |
| R3 | Dependencies | Frontend, MCP, or build-tool updates change Node engine requirements or leave current advisories unresolved | CI/deploy can fail after local success, vulnerable request/parsing chains can remain reachable, or a release can ship despite a non-zero gate | WO-025 established zero-audit policy. The 2026-08-20 review finds MCP at 1 moderate/4 high (`hono`, `brace-expansion`, `fast-uri`, `ip-address`, `js-yaml`) and frontend at 1 high (`nanoid`). WO-035 starts with a separate dependency/lockfile iteration, then reruns affected/full suites and all four committed audits at zero before any release mutation. | Security and Privacy Reviewer + Ops/Release | Reopened; explicit WO-035 hard release blocker, no waiver |
| R4 | Benchmark site data | Vite public asset paths or `site-data/` layout drift from frontend fetch paths | Metrics pages render empty or fail at runtime | Verify `/cortex/bootstrap/index.json` and selected summary/repo JSON after deploy-affecting changes | Frontend and Benchmarks | Open watch item |
| R5 | Optional parser toolchain | Optional language parser dependencies such as `dotnet` are unavailable locally | `cortex update` reports warnings and language coverage may be incomplete | Treat parser availability warnings as validation notes; require environment-specific follow-up only when touched work depends on that parser | Parsers and Ingest | Accepted baseline |
| R6 | Benchmark coverage | Angular-style TypeScript repos can look indexed while most semantic structure is missing | MCP context quality is poor despite successful bootstrap runs | WO-001 and WO-004 implemented 2026-06-16. Execute WO-002 and WO-003 for relation/resource quality; rerun benchmark evidence after those land. | Parsers and Ingest | Partially mitigated |
| R7 | Monorepo scope | Auto-detected source paths can include broad generated, docs, examples, and third-party areas | Embedding time and graph size rise while relevant code signal is diluted | Execute WO-005; surface detected scope and skipped-extension diagnostics before changing defaults | CLI and Scaffold | Open |
| R8 | Memory | Bootstrap phases can retain whole-repo entity, relation, embedding, and file-content structures at once | Large repos can OOM or regress despite faster algorithms | First pass implemented 2026-06-16: bootstrapbench RSS gate, graph CSV streaming, embedding JSONL streaming, ingest output/worker memory reductions. WO-013 removed full worker-result retention at completion. Next measured target is token/rule-match and post-parse materialization; later work can cover streaming `GraphData`, lazy embed entity loading/search top-k, and C# batch caps. | Control Manager | Partially mitigated |
| R9 | Integration | Stale perf draft branches can be merged after main moved on | Regressions or deleted site/benchmark work can re-enter through an outdated branch | Branch audit completed 2026-06-16: `perf/embed-scheduler` already in `main`; `perf/bootstrap-stage-optimizations` is stale/superseded and should not be merged. | Control Manager | Mitigated locally |
| R10 | Validation | Performance PRs can show runtime wins while hiding memory regressions | CI may pass but user machines/containers fail on large repos | Bootstrapbench now samples and aggregates peak RSS; Cortex/Angular runs `memory-rss-trace-2026-06-16` and `memory-rss-stream-2026-06-16` succeeded 2/2. Remaining gap: official Docker runner does not forward `CORTEX_INGEST_TRACE_MEMORY`; manual trace was used. | Validation | Partially mitigated |
| R11 | Product migration | Removing MCP defaults before CLI retrieval has parity can strand existing users and weaken agent context quality | `cortex init --bootstrap` or existing Codex/Claude workflows may stop working, and agents may fall back to raw file reads | WO-016 through WO-019 implemented locally: CLI retrieval parity exists, MCP client registration is opt-in, `.context/mcp` remains the compatibility runtime path, and docs/package/release guidance now says CLI-first with MCP compatibility. Future package rename, `cortex mcp` removal, `.context/mcp` removal, or registry support removal requires a breaking-release or parallel-package plan. | Control Manager | Mitigated locally |
| R12 | Semantic quality | Token compression, truncation, or retrieval-reranking changes can make Cortex faster or more diverse while losing important semantic signal | Cortex may look faster in benchmarks but return worse context to agents on real tasks | WO-020 now keeps compression behind explicit `CORTEX_EMBED_TEXT_PROFILE=compact-files`, compacts only large file-level records, preserves chunk text, and exposes profile/cache/manifest state. Cortex/bat quick gate and Angular capped-control gate had 0 lost expected hits. Cortex query pack was later calibrated to remove broken expected paths; accepted retrieval ranking improved Cortex to 29/35 and Angular to 22/42. Follow-up path/aspect/inflection experiments regressed and were rejected. The damaged Cortex/Angular benchmark workspaces were rebootstraped and sanity-checked back to 29/35 and 22/42 on 2026-06-25; rerun compact-files against the calibrated Cortex gate and run fastjson2 before promoting the profile beyond experimental opt-in. | Embedding and Validation | Partially mitigated |
| R13 | Enterprise security | Remote skill identifiers and persisted paths could escape managed roots; license/cache/secret identity was weak; privileged commands trusted project runtime/state; per-user daemon services could cross project boundaries | Arbitrary root/user filesystem mutation, credential exposure or cross-org artifact reuse, and missing or misattributed governance | WO-024 adds strict ownership/containment, credential-bound cache/artifacts/events, TLS-only bearer egress, stdin-only atomic `0600` onboarding, package-owned privileged code with target-user project I/O, verified daemon control, and separated project/host services. Existing users explicitly re-enroll; legacy unmarked skills require reviewed backup/reconciliation. | Security and Privacy | Mitigated locally in 2.4.1; security and release reviews have no blocker/major findings |
| R14 | Refactor integration | Moving CLI and ingest responsibilities can silently change command streams, exit codes, JSON envelopes, trusted Enterprise resolution, deterministic output, fallback behavior, or peak memory | Existing automation may break, Enterprise trust could regress, or large repositories could produce different context or fail | WO-026 froze the executable 2.4.1 contract; WO-027 through WO-030 preserved it while modularizing CLI/ingest and adding safe scaffold ownership. WO-031 reran syntax/focused/full suites, every frozen digest/equivalence/trace contract, extracted package bootstrap/doctor/update/search/force smokes, and six comparable memory samples. The final 2.4.2 package has 417 entries and all six reviewers closed clean. | Control Manager + Validation | Mitigated locally through WO-031 |
| R15 | Scaffold ownership | Forced upgrades overlay new generated files but cannot distinguish obsolete Cortex-managed files from unknown user files | Stale runtime sources can break bootstrap, while broad cleanup could delete user configuration or secrets | WO-030 ships a versioned 380-file ownership manifest, installed state/fingerprints, hash-pinned `2.4.1` pre-state bridge, protected-file policy, contained non-recursive cleanup, symlink/traversal/live-collision denial, atomic hard-link-safe replacement, and stale/unknown/modified/secret migration fixtures. WO-031 confirmed the extracted artifact removes an exact legacy file, persists 380 fingerprints, preserves unknown/config/ontology/agent/Enterprise content, and repairs mode `0600`. Portable Node APIs retain the documented narrow same-user ancestor-swap assumption. | CLI and Scaffold + Security and Privacy | Mitigated locally through WO-031 |
| R16 | Ingest filesystem containment | v2.4.2 allowed absolute/parent/symlinked source escapes and redirected ingest/dashboard cache, DB, manifest, relation, and npm-cache access | Accepted WO-033 contains source/control/worker/dashboard-source reads. Accepted WO-034 contains all seven prior caches and all 48 outputs behind exclusive whole-set staging, a complete precommit pass, manifest-last commit, hard-link-safe rename/cleanup, and all-before-any dashboard data/npm preflight with policy propagation; no new source-data egress, telemetry, or network path is added. | The 41-case boundary matrix covers every owned identity as safe/missing/link/wrong-type/special, late final/parent/stage replacement, hard links, lifecycle, counters, and both dashboards. Frozen/root/MCP, real packed full-output, Cortex gates, and all independent re-reviews are green. WO-035 full packed negative acceptance/release classification and R3 audit remediation remain mandatory. | Security and Privacy + Parsers and Ingest | Open through WO-035; WO-034 accepted locally |
